const config = {
    APPLICATION: {
        PORT: 3000,
    },
    DATABASE: {
        PORT: 3306,
        HOST: 'localhost',
        NAME: 'bts_database',
        USER: 'bts_user',
        PASSWORD: 'bts_password',
    }
}

module.exports = config