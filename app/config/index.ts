const config = {
    JWT: {
        SECRET: 'my-secret'
    },
    APPLICATION: {
        PORT: 3000,
    },
    DATABASE: {
        PORT: 3306,
        HOST: 'localhost',
        NAME: 'bts_database',
        USER: 'bts_user',
        PASSWORD: 'bts_password',
    }
}

export default config